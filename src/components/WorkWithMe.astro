---

---

<section class="contact-section">
    <div class="work-with-me-container">
        <div class="cta-wrapper">
            <h2 class="contact-heading" id="contact-heading-flashlight" data-i18n="contact.cta">Let's work together</h2>
            <a
                href="https://wa.me/972505992409?text=Hi%20Michael%2C%20I%27d%20like%20to%20work%20together."
                data-i18n-href="contact.wa.link"
                class="work-with-me"
                target="_blank"
                rel="noopener noreferrer"
            >
                <span data-i18n="contact.whatsapp">WhatsApp Me</span>
            </a>
        </div>
    </div>

    <div class="contact-details">
        <p class="contact-label" data-i18n="contact.label">Get in Touch</p>
        <div class="contact-links">
            <a
                href="mailto:Michael.Yudin@protonmail.com"
                class="contact-link"
                id="contact-email"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <rect x="2" y="4" width="20" height="16" rx="2"></rect>
                    <path d="M22 4L12 13L2 4"></path>
                </svg>
                <span data-i18n="contact.email"
                    >Michael.Yudin@protonmail.com</span
                >
            </a>

            <a
                href="https://wa.me/972505992409?text=Hi%20Michael%2C%20I%27d%20like%20to%20work%20together."
                data-i18n-href="contact.wa.link"
                class="contact-link wa-link"
                id="contact-wa"
                target="_blank"
                rel="noopener noreferrer"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"></path>
                </svg>
                <span data-i18n="contact.whatsapp">WhatsApp Me</span>
            </a>
            <a
                href="https://www.michaeltech.net"
                target="_blank"
                rel="noopener noreferrer"
                class="contact-link"
                id="contact-website"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <circle cx="12" cy="12" r="10"></circle>
                    <path d="M2 12h20"></path>
                    <path
                        d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"
                    ></path>
                </svg>
                <span data-i18n="contact.website">michaeltech.net</span>
            </a>
            <a
                href="https://www.michaeltech.net"
                target="_blank"
                rel="noopener noreferrer"
                class="contact-link"
                id="contact-location"
            >
                <svg
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                >
                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"
                    ></path>
                    <circle cx="12" cy="10" r="3"></circle>
                </svg>
                <span data-i18n="contact.location">Harish, Israel</span>
            </a>
        </div>
    </div>
</section>

<style>
    .contact-section {
        padding: 40px 0 20px;
        width: 100%;
        position: relative;
        z-index: 10;
        animation: fadeUp 0.8s ease-out 0.2s backwards;
    }

    .work-with-me-container {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 40px 0 60px;
        width: 100%;
        text-align: center;
    }

    .cta-wrapper {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 24px;
    }

    .contact-heading {
        font-family: "Fraunces", serif;
        font-size: clamp(2rem, 6vw, 3.5rem);
        font-weight: 300;
        margin: 0;
        transition: opacity 0.8s ease;
        cursor: default;
        user-select: none;

        /* Flashlight gradient text */
        color: transparent;
        background-color: #333;
        background-image: radial-gradient(
            circle 150px at var(--grad-x, 50%) var(--grad-y, 50%),
            #c9a227 0%,
            #c9a227 20%,
            #444 60%
        );
        background-size: 100% 100%;
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        will-change: background-position, background-image;
    }

    .work-with-me {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 16px 40px;
        font-size: 1.25rem;
        font-weight: 500;
        text-decoration: none;
        border-radius: 12px;
        cursor: pointer;
        opacity: 0;
        transition: all 0.3s ease;
        position: relative;
        color: var(--background);
        background: var(--accent);
        box-shadow: 0 4px 20px -4px rgba(201, 162, 39, 0.5);
    }

    .work-with-me:hover {
        transform: translateY(-4px);
        box-shadow: 0 8px 30px -4px rgba(201, 162, 39, 0.7);
    }

    .contact-details {
        text-align: center;
        padding: 40px 0 0;
        border-top: 1px solid var(--card-border);
    }

    .contact-label {
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        color: var(--accent);
        margin-bottom: 24px;
        font-weight: 600;
    }

    .contact-links {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 32px;
        flex-wrap: wrap;
    }

    .contact-link {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        color: var(--text-muted);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 400;
        padding: 10px 16px;
        border-radius: 10px;
        transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
    }

    .contact-link::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: 10px;
        background: rgba(201, 162, 39, 0.06);
        opacity: 0;
        transition: opacity 0.35s ease;
    }

    .contact-link:hover {
        color: var(--accent);
        transform: translateY(-2px);
    }

    .contact-link:hover::before {
        opacity: 1;
    }

    .contact-link svg {
        width: 18px;
        height: 18px;
        flex-shrink: 0;
        transition: color 0.35s ease;
    }

    .contact-link:hover svg {
        filter: drop-shadow(0 0 6px rgba(201, 162, 39, 0.4));
    }

    .wa-link {
        color: var(--accent);
    }

    .wa-link::before {
        background: rgba(201, 162, 39, 0.1);
    }

    @media (max-width: 768px) {
        .contact-links {
            flex-direction: column;
            gap: 8px;
        }

        .contact-link {
            font-size: 0.9rem;
        }
    }

    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>

<script>
    const el = document.getElementById("contact-heading-flashlight");

    if (el) {
        // State
        let mouseX = 0;
        let mouseY = 0;
        let currentX = 50;
        let currentY = 50;

        let clientWidth = 0;
        let clientHeight = 0;

        const LERP_FACTOR = 0.08;

        const updateDimensions = () => {
            if (!el) return;
            const rect = el.getBoundingClientRect();
            clientWidth = rect.width;
            clientHeight = rect.height;
        };

        updateDimensions();
        window.addEventListener("resize", updateDimensions);

        window.addEventListener("mousemove", (e) => {
            if (!el) return;
            const rect = el.getBoundingClientRect();
            mouseX = e.clientX - rect.left;
            mouseY = e.clientY - rect.top;
        });

        let isMobile = window.matchMedia("(max-width: 768px)").matches;
        let time = 0;

        function animate() {
            if (!el) return;
            if (isMobile) {
                time += 0.003;
                const rect = el.getBoundingClientRect();
                const w = rect.width || 200;
                const h = rect.height || 50;

                const targetX = w / 2 + Math.cos(time) * (w * 0.4);
                const targetY = h / 2 + Math.sin(time * 0.7) * (h * 0.8);

                currentX += (targetX - currentX) * 0.05;
                currentY += (targetY - currentY) * 0.05;
            } else {
                currentX += (mouseX - currentX) * LERP_FACTOR;
                currentY += (mouseY - currentY) * LERP_FACTOR;
            }

            el.style.setProperty("--grad-x", `${currentX}px`);
            el.style.setProperty("--grad-y", `${currentY}px`);

            requestAnimationFrame(animate);
        }

        animate();
    }
</script>
